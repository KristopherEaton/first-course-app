import { Component } from '@angular/core';

// Pass variable to configure component, selector = name etc...
@Component({
  selector: 'app-server',
  templateUrl: './server.component.html',

})
export class ServerComponent {}